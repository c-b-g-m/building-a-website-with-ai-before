# Bayou Brew Coffee — earlier version

An earlier version of the Bayou Brew Coffee website. Three plain HTML pages with inline styles.

## Pages

- `index.html` — home + menu
- `about.html` — about the shop
- `contact.html` — visit info + contact form

## Notes

Older static HTML. Self-contained `<style>` blocks per page. Kept around as a reference snapshot.
